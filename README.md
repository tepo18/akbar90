# Trojan Worker Custom

## How to Use:

1. **Replace** `REPLACE_WITH_YOUR_UUID` in the `worker.js` file with your **actual UUID** from your Cloudflare account.
   - Go to [Cloudflare](https://www.cloudflare.com/) and sign in.
   - Under **Workers**, find your **UUID**.
   
   **Example:**
   ```js
   const uuid = "REPLACE_WITH_YOUR_UUID";  // Replace this with your actual UUID
